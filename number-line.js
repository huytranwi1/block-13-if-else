// given two inputs, we wil be expected to return the "sum" + string
 // first we will create two function that take in value that can change

 // then we will sum it together

 // once getting the values we will print the following sum plus string.
                        // sum + " is less than -1000";
                        // sum + " is a negative number";
                        // sum + " is equal to 0";
                        // sum + " is larger than 0";
                        // sum + " is greater than 100";


let num1 = x;

let num2 = y;

let sum = num1 + num2;

// we will run 2 test

let num1 = 50;
let num2 = 51;
let sum = num1 + num2;

if (num1 === 50 && num2 === 51) {
    console.log (sum + "is greater than 100")
}