// compares two sets of two values. 
// Print true if at least one of the pairs is truthy.

// this question we will use the "OR" operator to find 1 true statement or "AND" and both value must be true

// we need to create four variable that take in four given value.


// const param1A = x;
// const param1B = y;

// const param2A = c;
// const param2B = d;

// find out if true or false

const param1A = "cat";
const param1B = "cat";

const param2A = 6;
const param2B = "6";

if (param1A === param1B || param2A === param2B) {
    console.log("true")
} else {
    console.log("False")
}
